from base import *


# EMAIL_HOST = 'hvzmail.com'
# EMAIL_HOST_USER = 'robot@hvzmail.com'
# EMAIL_HOST_PASSWORD = 'DFS9JEo8Ju8L'
# EMAIL_USE_TLS = 0
# EMAIL_DEBUG = 1


CONTACT_EMAIL = "Hosting@HvZSOURCE.com"
DEFAULT_FROM_EMAIL = "Robot@HvZMail.com"

EMAIL_HOST = 'hvzmail.com'
EMAIL_PORT = 25
EMAIL_HOST_USER = 'robot@hvzmail.com'
EMAIL_HOST_PASSWORD = 'PXlXkQlN0HCn'
EMAIL_USE_TLS = 0
EMAIL_DEBUG = False


DEBUG = False


import json
with open('/home/dotcloud/environment.json') as f:
  env = json.load(f)

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': 'source',
        'USER': env['DOTCLOUD_DATA_SQL_LOGIN'],
        'PASSWORD': env['DOTCLOUD_DATA_SQL_PASSWORD'],
        'HOST': env['DOTCLOUD_DATA_SQL_HOST'],
        'PORT': int(env['DOTCLOUD_DATA_SQL_PORT']),
    }
}


# DATABASE_ENGINE = 'postgresql_psycopg2'    # 'postgresql_psycopg2', 'postgresql', 'mysql', 'sqlite3' or 'ado_mssql'.
# DATABASE_NAME = 'source'       # Or path to database file if using sqlite3.
# DATABASE_USER = 'source'             # Not used with sqlite3.

# DATABASE_PASSWORD = 'K39dfjLcZ32K93'         # Not used with sqlite3.
# DATABASE_HOST = '1680be1e.dotcloud.com'             # Set to empty string for localhost. Not used with sqlite3.
# DATABASE_PORT = '12871'             # Set to empty string for default. Not used with sqlite3.


DEFAULT_FILE_STORAGE="storages.backends.s3boto.S3BotoStorage"


AWS_ACCESS_KEY_ID = 'AKIAJTHG75GEP44NPXMA'
AWS_SECRET_ACCESS_KEY = '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'
AWS_STORAGE_BUCKET_NAME = 'hvz'
AWS_HEADERS = {
    'Expires': 'Tue, 14 Aug 2012 20:00:00 GMT',
    'Cache-Control': 'max-age=8640000',
    }

HTTPS_SUPPORT=True
