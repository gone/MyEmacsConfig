Slader.Theme = SC.AceTheme.create({
  // This name will identify the theme. Make it good. :)
    name: 'knightrider',
});
 
// Tell SproutCore about your theme
SC.Theme.addTheme(Slader.Theme);
 
// Make it the default theme
SC.defaultTheme = 'knightrider'