import random
from django import template

register = template.Library()

@register.inclusion_tag('payment/includes/socialgold_point_balance.html', takes_context=True)
def get_user_point_balance(context):
    request = context['request']
    user = request.user
    rand = random.random()
    return {'user':user,
            'random':rand}


